# Critique ignores — single post / reading page

# Lines below are findings the operator has reviewed and chosen
# not to re-raise. Future runs of /impeccable critique drop any
# finding whose rule name or snippet substring-matches a line here
# (case-insensitive). Comments (lines starting with #) and empty
# lines are skipped.

# The Smaug-Red drop cap on titled reading scrolls is a deliberate
# wonder-moment that is working as intended. Selector fragility
# (`:first-of-type` → `:first-child`) has been fixed; the visual
# choice is on-brand.
drop cap reads as a second wonder-moment
drop cap as a second wonder-moment
Russian-doll wonder

# The 60px hairline-colored fleuron at the article foot is the
# inscription's deliberate close mark. The operator has chosen to
# keep it; reviewers are welcome to flag related concerns
# (composition, spacing, sizing) but the existence of the colophon
# in this position is settled.
article-foot colophon SVG sits in dead air
inner-article colophon
colophon fleuron sits in dead air

# The drop cap fires on whatever the first paragraph is, including
# the chronicle author's chatty preamble openings (a soup gag, a
# parenthetical aside, an apostrophe to the reader). This is the
# author's actual voice, not a content-vs-design tension to fix —
# the cap's job is "mark the opening," and the opening is whatever
# the inscription opens with. Reviewers who flag the cap landing
# on a non-substantive lead paragraph are reading the page as a
# magazine article where the lede should be the thesis; this is a
# personal chronicle where the lede is the voice.
drop cap fires on a "soup" gag
drop cap fires on whatever the first paragraph
cap fires on whatever the first paragraph
cap fires on a chatty preamble
drop cap conflicts with the prose first-paragraph
